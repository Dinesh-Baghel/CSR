using System;

namespace Application
{
    public class Architecture
    {
//MyAppSolution
//│
//├── MyApp.API              → Presentation Layer(controllers, startup, middleware)
//├── MyApp.Application      → Application Layer(CQRS, interfaces, validation)
//├── MyApp.Domain           → Domain Layer(entities, enums, domain logic)
//└── MyApp.Infrastructure   → Infrastructure Layer(repositories, Dapper, JWT, etc.)

    }
}
